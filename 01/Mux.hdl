// output = a !b !s + a b !s + !a b s + a b s
// = a !s (b + !b) + b s(!a + a)
// since x + !x = 1
// output = a !s + b s

CHIP Mux {
	IN a, b, sel;
	OUT out;
	PARTS:
	Not(in=sel, out=notsel);
	And(a=a, b=notsel, out=left);
	And(a=b, b=sel, out=right);
	Or(a=left, b=right, out=out);
}

